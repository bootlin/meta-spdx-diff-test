header:
  version: 14
  includes:
    - kas/base-layers.yml
    - kas/distro.yml
    - kas/base.yml

target:
  - core-image-minimal

local_conf_header:
  baseimages: |
    IMAGE_INSTALL:append = " zstd"
